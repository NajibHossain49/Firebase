// eslint-disable-next-line no-unused-vars
import React from 'react';

const Header = () => {
    return (
        <div>
            <p>Header...</p>
        </div>
    );
};

export default Header;